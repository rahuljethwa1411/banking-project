import React from "react";

const CreditDetails = ({ card }) => {
  return (
    <div className="card-details p-3 rounded text-center bg-primary bg-gradient ms-5">
      {/* No content is displayed here */}
    </div>
  );
};

export default CreditDetails;